---
# ServiceAccount for the test app.
# Vault role "myapp" is bound to this SA name + namespace (see 01-vault-setup.sh).
# The injector uses the projected token automatically mounted by K8s to authenticate.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: myapp-sa
  namespace: default
