apiVersion: 1

providers:
  - name:            default
    orgId:           1
    folder:          Logging
    folderUid:       logging
    type:            file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
